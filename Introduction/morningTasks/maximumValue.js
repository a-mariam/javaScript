Array.prototype.maximumValue =  function (){
    let check = false
    let first = this[o].length
    for(let i = 0; i < this.length; i++){
        if(Number(this[i])  == Number ) check = true
        if(check == false){
            if(Number(this[i]).length > first) {
                first = Number(this[i])
                return first
            }
        }
    }
}